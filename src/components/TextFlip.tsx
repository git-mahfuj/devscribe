import { ContainerTextFlip } from "@/components/ui/container-text-flip";

export function FlipedText() {
  return (
    <ContainerTextFlip
      words={["Code", "<html>", "Return()", "stdio.h"]}
    />
  );
}
